"check-mate" flag - must set when appropriate
"stalemate" flag - must set when appropriate
heuristics

need to log moves to the log _Do we need to do this or is this already taken care of by the profiler(framework)?

have GetNextMove continually generate moves until the turn is over. 
